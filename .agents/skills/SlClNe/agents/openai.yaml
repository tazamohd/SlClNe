interface:
  display_name: "SlClNe"
  short_description: "Repo-specific patterns and workflows for SlClNe"
  default_prompt: "Use the SlClNe repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true