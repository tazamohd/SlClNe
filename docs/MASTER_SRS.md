# SALIS AUTO — Software Requirements Specification

**Version** 1.1 · **Date** 2026-09-03 · **Status** DRAFT, pending review

This document specifies what SALIS AUTO must do and how conformance is proven.
It is written against the code as it stands on 2026-09-03 and cites file paths so
that any claim here can be checked rather than believed.

Where a requirement is met, this document says so and names the evidence. Where
it is not, it says that instead. A specification that reports a system as more
finished than it is costs more than no specification, because it removes the
reason to look. §9 lists every place the existing documentation and the code
currently disagree.

---

## 1. Introduction

### 1.1 Purpose

SALIS AUTO is a multi-tenant automotive service management platform for the Saudi
market: workshop operations, parts and inventory, accounting and ZATCA-compliant
invoicing, CRM, HR, procurement, insurance and loans, plus customer, supplier,
technician and procurement portals and a public marketing site.

This SRS is the contract between what is designed, what is built and what is
verified. It exists because three sources of truth had drifted apart — the design
bundle, the generated registry, and the running code — and a reader could not
tell which to believe.

### 1.2 Scope

In scope: the React application in `app/`, the Fastify API in `server/`, the
shared contract in `packages/contract/`, and the generated capability registry
that governs both.

Out of scope: the design prototypes in `project/` (they are input, not
deliverable), infrastructure provisioning, and the twelve capabilities in §2.6
that depend on hardware or third-party platforms not yet contracted.

### 1.3 Definitions

| Term | Meaning |
|---|---|
| **Capability** | The unit of completion. "Customer management" is a capability; `Customers.tsx` is not. |
| **Registry** | `project-control/MASTER_REGISTRY.json`, generated by `app/scripts/build-registry.mjs`. The authority on which capabilities exist and their state. |
| **Surface** | A distinct application shell: app, auth, public, customer-app, portal, kiosk, call-center, native, reference. |
| **Module** | One of 28 RBAC permission scopes. Screens map to modules via `SCREEN_MODULE`. |
| **Principal** | `{userId, orgId, branchId, role, scope}` — the identity every server query is executed under. |
| **Halala** | 1/100 SAR. All monetary values are stored as integer halalas. |

### 1.4 Authority

Where sources conflict, this order governs:

1. **The code and its passing tests** — what the system does.
2. **The registry** (`project-control/*.json`) — generated, not written.
3. **`packages/contract/`** — the schema both sides import.
4. This SRS and the `docs/MASTER_*` set — prose, and prose goes stale.

A count written into a document is a dated snapshot. Counts in this document
carry the date they were taken.

---

## 2. Overall Description

### 2.1 Product perspective

Three deployable parts and one shared library:

```
app/       React 18 · Vite 5 · TypeScript 5.7 · Tailwind 3.4
           TanStack Query (server state) · Zustand (session) · React Router 6

server/    Node 20 · Fastify 5 · Drizzle ORM · PostgreSQL 16 with row-level security
           argon2id · jose (JWT) · Zod

packages/  contract/ — Zod schemas, RBAC tables and business rules,
contract   imported by BOTH sides so one definition is the API type,
           the server guard and the form validator
```

The client reaches data through a repository seam (`app/src/data/repository.ts`).
`Collection<T>` has one shape; a fixture implementation and an HTTP
implementation both satisfy it, selected by `VITE_API_BASE_URL`. Screens do not
change when the data source does. This seam is the single most important
architectural property of the system and every requirement about swapping data
sources depends on it holding.

### 2.2 Capability inventory

As of 2026-09-03, from `project-control/STATUS.json`:

| | Count | Change since 2026-09-02 |
|---|---|---|
| Capabilities registered | **424** | — |
| — product | 384 | — |
| — reference-only (design-system documentation) | 28 | — |
| — external dependency (§2.6) | 12 | — |
| Rendering a real component | 424 | — |
| Placeholder routes | 0 | — |
| Backed by real data | **99** | +99 |
| Reading fixtures only | **285** | −99 |
| Built screens owing a designed mobile layout | 1 | +1 |

The first ninety-nine capabilities crossed from fixtures to the API in a day,
which is what the repository seam was built for: no screen moved.

By surface: app 313 · public 31 · auth 28 · reference 28 · customer-app 11 ·
portal 8 · call-center 2 · native 2 · kiosk 1.

By domain: featuremap 211 · website 31 · auth 28 · ui 28 · accounting 23 ·
workshop 22 · admin 18 · crm 17 · portals 13 · customerapp 11 · ai 10 ·
parts 10 · hr 1 · procurement 1.

### 2.3 User classes

Fourteen roles, defined in `packages/contract/src/rbac.ts` and enforced through
28 modules × 5 actions (view, create, edit, delete, approve).

| Class | Roles | Scope | Primary need |
|---|---|---|---|
| Ownership | owner, superadmin | all / platform | Oversight, approval above every ceiling, configuration |
| Operations | manager, advisor, frontdesk, callcenter | branch | Booking, job progression, customer contact |
| Workshop floor | technician, qc | own | Executing and inspecting work; a technician sees their own jobs |
| Supply | parts, procurement | branch | Stock, requisitions, purchase orders, supplier network |
| Finance & people | accountant, hr | all / branch | Invoicing, payments, ledger, payroll, leave |
| External | supplier, customer | external | Portal-scoped self-service only |

**Financial approval ceilings** (SAR, enforced by `canApprove()`): owner ∞ ·
superadmin ∞ · manager 50,000 · accountant 25,000 · hr 15,000 · parts 10,000 ·
advisor 5,000 · all others 0.

### 2.4 Operating environment

- **Client**: evergreen Chrome, Firefox and Safari; viewports 320–1920 px;
  English and Arabic; light and dark; touch and keyboard.
- **Server**: Node 20, PostgreSQL 16. The application connects as a
  **non-superuser** role so that row-level security applies to it
  (`server/scripts/migrate.ts` creates `salis_app` deliberately without
  `BYPASSRLS`).
- **Locale**: Saudi Arabia. SAR currency, ZATCA tax rules, Arabic as a
  first-class language rather than a translation layer.

### 2.5 Constraints

| Constraint | Origin |
|---|---|
| Brand permits blue and orange only; green and purple are forbidden | Design system; gated by `app/scripts/check-tokens.mjs` |
| No financial value computed in the browser and trusted | `docs/MASTER_BUSINESS_RULES.md` |
| Money stored as integer halalas, formatted only at the boundary | Architecture |
| Nothing under `app/src/data/generated/` is hand-edited | Three generators own it |
| Largest JS chunk ≤ 375 kB raw | `app/scripts/check-bundle.mjs` |
| No placeholder may ship on a product route | `app/scripts/check-no-fake.mjs` |

### 2.6 Assumptions and dependencies

Twelve registered capabilities depend on hardware or platforms that do not exist
in this system and are **not** commitments of this specification:
Wearable-Integration, Voice-Commands, Voice-Command-Interface, Drone-Inspection,
AR-Repair-Guide, AR-Overlay, VR-Showroom, Blockchain-Service-History,
Smart-Contracts, Quantum-Computing, Digital-Signage, Security-Cameras.

They render the generic `FeatureScreenView` kit today. Three source files are
retained unrouted as build references for them, listed in `RETAINED_REFERENCE`
in `app/scripts/build-registry.mjs`; every other unreachable screen file is a
defect.

---

## 3. Functional Requirements

Requirements are stated as obligations on the system. **Status** is one of
*Met* (implemented and covered by a passing test), *Partial*, or *Not met*.

### 3.1 Identity and session

| ID | Requirement | Status |
|---|---|---|
| SRS-AUTH-1 | Authenticate by email and password, argon2id hashed. Default parameters: 19,456 KiB memory, time cost 2, parallelism 1 (`server/src/auth/config.ts`). | Met |
| SRS-AUTH-2 | Issue a JWT access token, default TTL 15 minutes, carrying `{sub, role, org_id, branch_id, scope}`. | Met |
| SRS-AUTH-3 | Issue a rotating refresh token, default TTL 30 days, with reuse detection that revokes the session family. | Met |
| SRS-AUTH-4 | Reject a wrong password and an unknown address identically, and pay the same verification cost, so the endpoint does not enumerate users. | Met — `verifyPassword` hashes a dummy on the miss path |
| SRS-AUTH-5 | Lock an account after 8 failed attempts for 300 seconds; rate-limit login to 10/minute and other auth endpoints to 20/minute. | Met |
| SRS-AUTH-6 | Require passwords of at least 12 characters. | Met — `checkPasswordPolicy` |
| SRS-AUTH-7 | Never place a password, hash or token digest in any response or log. | Met — asserted in `auth.test.ts`; `logger.ts` redacts |
| SRS-AUTH-8 | Support OTP verification: 10-minute TTL, 5 attempts, 60-second resend floor. | Met |
| SRS-AUTH-9 | Seeded demo identities carry **no** password hash; credentials are set through the auth service, never seeded. | Met, by design |
| SRS-AUTH-10 | Support SSO (OIDC) and WebAuthn. | **Not met** — configuration keys exist and default to empty; the flows are UI-only stubs |

### 3.2 Authorization

| ID | Requirement | Status |
|---|---|---|
| SRS-AUTHZ-1 | Every screen maps to exactly one of 28 RBAC modules, or is explicitly listed as ungated. | Met — `SCREEN_MODULE`; 16 ungated screens enumerated |
| SRS-AUTHZ-2 | The client must refuse to render a screen the role has no view grant for. | Met — `RequireAccess` |
| SRS-AUTHZ-3 | The server must independently enforce the same matrix. Client-side checks are convenience and carry no security weight. | Met — `server/src/auth/rbac.ts`; `rbac-parity.test.ts` asserts the server matrix equals the client's |
| SRS-AUTHZ-4 | An action above the role's SAR ceiling must escalate rather than execute. | Met — `canApprove()` |
| SRS-AUTHZ-5 | Enforce 6 separation-of-duty rules; a technician may not approve their own QC. | Met |
| SRS-AUTHZ-6 | Redact 7 field-level rules by role — part cost and margin are hidden from advisors, technicians and customers. | Met client-side; server-side redaction **Partial** |
| SRS-AUTHZ-7 | 219 capabilities currently map to no RBAC module. | **Not met** — flagged `NO_RBAC_MODULE` in the registry |

### 3.3 Multi-tenancy and data scope

| ID | Requirement | Status |
|---|---|---|
| SRS-TENANT-1 | Every tenant-owned table carries `org_id`, and `branch_id` where branch scope applies. | Met — 69 tables in `server/src/db/schema.ts` |
| SRS-TENANT-2 | Isolation is enforced by PostgreSQL row-level security, not by a `WHERE` clause a developer must remember. | Met — 22 tables with RLS enabled, 43 policies across `server/drizzle/0001_rls.sql` and later migrations |
| SRS-TENANT-3 | Each request sets `app.org_id`, `app.branch_id`, `app.user_id` and `app.scope` as transaction-local settings, which the policies read. | Met — `server/src/db/tenant.ts` |
| SRS-TENANT-4 | A cross-tenant read or write must return no rows, not a filtered subset assembled in application code. | Met — `isolation.test.ts` |
| SRS-TENANT-5 | The `own` scope restricts a technician to their own assigned work. | Met — `drizzle/0002_own_scope_tech.sql` |

### 3.4 Business domains

Each domain's rules live once in `packages/contract/src/rules/` and are enforced
server-side, surfaced as client validation, and unit-tested. A rule that lives
only in a component is a rule a second component will contradict.

| ID | Domain | Requirement | Status |
|---|---|---|---|
| SRS-WS-1 | Workshop | Job cards progress through a server-side state machine; an invalid transition is refused with 409. | Met server-side |
| SRS-WS-2 | Workshop | The check-in → inspection → estimate → QC → signature → delivery chain is enforced in order. | Met |
| SRS-FIN-1 | Money | `Invoice.total = subtotal + tax − discount`, computed by the server. | **Partial** — derived in `InvoiceCreate` client-side; server enforcement incomplete |
| SRS-FIN-2 | Money | VAT computed at the ZATCA rate server-side, never accepted from the client. | Met in `rules/money.ts` |
| SRS-FIN-3 | Money | `Payment.amount ≤ invoice.balance`; a cancelled invoice refuses a normal payment; `Refund ≤ collected`. | Met in rules; covered by `finance-integrity` tests |
| SRS-FIN-4 | Money | Journal entry debits equal credits. | Met in rules |
| SRS-FIN-5 | Money | Rounding is half-up at two decimals, applied once at the total. | Met |
| SRS-INV-1 | Inventory | On-hand is derived from movements; a new part starts at an opening quantity and everything after is a movement. | Met |
| SRS-INV-2 | Inventory | Double-movement prohibitions and reservation integrity hold under concurrency. | Met — `inventory-enforcement.test.ts` |
| SRS-PROC-1 | Procurement | A requisition may not be both raised and approved by the same person. | Met — SOD rule |
| SRS-HR-1 | HR | Payroll and leave respect role scope and approval ceilings. | Met — `hr.test.ts`, `payroll.ts`, `leave.ts` |
| SRS-CRM-1 | CRM | Lead → opportunity → customer conversion preserves the audit chain. | Met |

### 3.5 Audit

| ID | Requirement | Status |
|---|---|---|
| SRS-AUDIT-1 | Every mutation writes an audit record naming actor, action, entity and request. | Met — `server/src/audit/audit.ts` |
| SRS-AUDIT-2 | Sensitive fields (`passwordHash`, `password_hash`, tokens) are never written to the audit trail. | Met — explicit redaction list |
| SRS-AUDIT-3 | A password change revokes every session opened with the old password. | Met |

---

## 4. External Interface Requirements

### 4.1 API

| ID | Requirement | Status |
|---|---|---|
| SRS-API-1 | All resources are served under `/api/v1`. Only `/health` and `/ready` sit at the root. | Met — `server/src/app.ts` |
| SRS-API-2 | Request and response schemas are Zod definitions in `packages/contract/`, imported by client and server. Neither side may define its own shape. | **Partial** — 20 entity modules exist; the client's fixture layer does not yet import them everywhere |
| SRS-API-3 | Errors use one envelope: `{error: {code, message, field?, requestId}}`. | Met |
| SRS-API-4 | Validation failures return 422 with the offending field; malformed requests 400; unknown resource 404; forbidden 403; unauthenticated 401; conflict 409. | **Partial** — see §9.3 |
| SRS-API-5 | Every mutating endpoint accepts an idempotency key and a replay returns the original result rather than acting twice. | **Partial** — `server/src/http/idempotency.ts` is wired into inventory, invoices and procurement only; the other 18 route modules do not use it |
| SRS-API-6 | List endpoints support filtering, free-text search, sorting and pagination, and reject an unknown filter field with 422. | Met server-side |

### 4.2 User interface

| ID | Requirement | Status |
|---|---|---|
| SRS-UI-1 | Every capability provides loading, empty, error-with-retry, success, permission-denied and read-only states. | **Partial** — 101 of 424 have a loading state, 98 empty, 69 error |
| SRS-UI-2 | An empty state is honest — never filled with invented rows. | Met — gated by `check-no-fake.mjs` |
| SRS-UI-3 | Large collections are paginated or virtualized; 10,000 rows never enter the DOM. | Partial |
| SRS-UI-4 | Nine shells exist and a capability renders in exactly the one the registry names. | Met |

### 4.3 Integrations

ZATCA e-invoicing, SMS and WhatsApp campaign transports, OBD diagnostics
ingestion, bank statement import, and S3-compatible file storage behind a
`StorageAdapter`. Present as route modules under `server/src/routes/` and
`server/src/integrations/`; external credentials are unconfigured in development.

---

## 5. Non-Functional Requirements

### 5.1 Performance

| ID | Requirement | Target | Status |
|---|---|---|---|
| SRS-PERF-1 | Largest JavaScript chunk | ≤ 375 kB raw | Met — gated by `check-bundle.mjs` |
| SRS-PERF-2 | The Arabic dictionary must not load for an English session | 0 bytes | Met — lazy chunk, ~236 kB raw |
| SRS-PERF-3 | Largest Contentful Paint | < 2.5 s | **Unverified** — no runtime measurement in CI |
| SRS-PERF-4 | Interaction to Next Paint | < 200 ms | **Unverified** |
| SRS-PERF-5 | Cumulative Layout Shift | < 0.1 | **Unverified** |
| SRS-PERF-6 | Animation is restricted to compositor-friendly properties | transform, opacity, clip-path | Met by convention; not gated |

### 5.2 Internationalisation and RTL

| ID | Requirement | Status |
|---|---|---|
| SRS-I18N-1 | Every literal UI string passes through `t()` and has an Arabic translation. | Met — 3,255 of 3,255 unique literal keys covered; gated by `check-i18n.mjs` |
| SRS-I18N-2 | Switching language must change direction and words in the same paint; RTL must never land on untranslated English. | Met — `setLanguage` resolves the dictionary before committing |
| SRS-I18N-3 | No physical-direction CSS utility; layout uses logical properties (`start-*`/`end-*`). | Met — gated by `check-logical-css.mjs`, currently clean |
| SRS-I18N-4 | Latin runs — plates, VINs, SKUs, money — stay LTR inside Arabic text. | Met — asserted in component tests |
| SRS-I18N-5 | Every `aria-label` is translated. | Met — gated by `check-a11y-copy.mjs`, currently clean |
| SRS-I18N-6 | Arabic and RTL verified per capability at runtime. | **Not met** — all 424 flagged `ARABIC_MISSING` and `RTL_BROKEN` in the registry; the gates above are static |

### 5.3 Accessibility

| ID | Requirement | Status |
|---|---|---|
| SRS-A11Y-1 | WCAG 2.2 AA, zero serious or critical violations. | **Partial** — static scan clean (8 rule classes, 0 findings); no runtime axe sweep |
| SRS-A11Y-2 | Every golden journey completable by keyboard alone. | Unverified |
| SRS-A11Y-3 | Focus visible; focus trapped and restored by modals. | Met |
| SRS-A11Y-4 | `prefers-reduced-motion` honoured wherever animation is used. | Met — gated statically |
| SRS-A11Y-5 | Touch targets ≥ 44 px. | **Partial** — 3 controls per screen measure under 44 px at every tablet width |

### 5.4 Responsive

| ID | Requirement | Status |
|---|---|---|
| SRS-RESP-1 | No horizontal overflow at 320, 375, 390, 430, 768, 820, 834, 1024, 1440, 1920. | Met for tablet widths — `e2e/tablet.spec.ts`, 65 checks passing |
| SRS-RESP-2 | Below 860 px the designed mobile layout renders — cards, not a narrowed table. | Met |
| SRS-RESP-3 | Rotation across the 860 px breakpoint re-evaluates the layout mid-session. | Met |
| SRS-RESP-4 | Desktop verified at 1280/1440/1536/1920. | Partial |

### 5.5 Security

**5.5.1 The API.** This process serves JSON and nothing else, so it can deny
every fetch directive outright.

| ID | Requirement | Status |
|---|---|---|
| SRS-SEC-1 | `X-Content-Type-Options: nosniff` | Met |
| SRS-SEC-2 | `Strict-Transport-Security` with a max-age | Met |
| SRS-SEC-3 | `X-Frame-Options: DENY` | Met — `frameguard: { action: 'deny' }`, verified on the wire |
| SRS-SEC-4 | `Content-Security-Policy: default-src 'none'` | Met — served as `default-src 'none';frame-ancestors 'none';base-uri 'none';form-action 'none'`. `useDefaults` is off: helmet's defaults are written for a page that loads scripts and styles, and this process serves JSON only |
| SRS-SEC-5 | `Referrer-Policy: no-referrer` | Met. **The requirement was tightened, not the status.** It previously read `strict-origin-when-cross-origin`; `no-referrer` sends no `Referer` at all, which is strictly stronger and costs an API nothing, so the specification now asks for what the code does because the code is right |
| SRS-SEC-6 | `X-Powered-By` absent | Met |
| SRS-SEC-7 | Rate limiting keyed by tenant and address, not address alone | Met |
| SRS-SEC-8 | No secret in source; secrets from environment only | Met — `.env` gitignored |
| SRS-SEC-9 | Parameterised queries throughout; user input escaped for `ILIKE` | Met — `escapeIlike` |
| SRS-SEC-10 | Exposed GitHub PATs rotated and secret scanning added to CI | **Not met** — BLK-003, open |

**5.5.2 The delivered application.** Added 2026-09-03. This half had no
requirements and no headers: nginx, Vercel and Netlify each served the built SPA
with none at all. It is the half that matters most, because it is the document a
browser parses and executes — where an XSS actually lands. The API being locked
down while the page loading it was not is the more dangerous of the two gaps,
and it was the one nobody had written down.

The headers are defined once in `app/security-headers.mjs`; `scripts/gen-headers.mjs`
writes them into all three deploy configs and `npm run check-headers` fails the
build when they drift.

| ID | Requirement | Status |
|---|---|---|
| SRS-SEC-11 | The built application is served with a Content-Security-Policy on every response, including static assets and error responses. | Met — `always` on every nginx `add_header`; the asset `location` repeats the set, because nginx discards inherited headers in a nested block |
| SRS-SEC-12 | `script-src 'self'` — no `'unsafe-inline'`, no `'unsafe-eval'`. | Met — the build emits zero inline `<script>`, so no nonce is needed. This is the directive that stops an injected script; weakening it makes the policy decorative |
| SRS-SEC-13 | `frame-ancestors 'none'` and `object-src 'none'`. | Met |
| SRS-SEC-14 | `font-src 'self'` — no external font host. | Met — Inter and Poppins self-hosted under `/fonts` |
| SRS-SEC-15 | `style-src` may use `'unsafe-inline'` only for as long as inline style attributes exist. | **Partial, with a deadline** — 95 source files use React's `style={{…}}`, which emits a style attribute and cannot carry a nonce. Narrow to `style-src-elem 'self'` once those are gone |
| SRS-SEC-16 | `connect-src` names every origin the application calls. | **Deployment-dependent** — `'self'` today. When `VITE_API_BASE_URL` points off-origin, that origin must be added or every request is blocked. The one directive a deployer must edit |
| SRS-SEC-17 | `Permissions-Policy` denies camera, microphone, geolocation and the rest of the hardware this application never requests. | Met |
| SRS-SEC-18 | HSTS for two years, subdomains included, preload-eligible. | Met |
| SRS-SEC-19 | The policy is exercised against the real build, not asserted. | Met — `e2e/security-headers.spec.ts` runs six specs against `vite preview`, which serves the production headers; it loads six real screens and the Arabic switch and fails on any CSP violation or page error |
| SRS-SEC-20 | One definition, three deploy targets, no drift. | Met — generated and gated by `check-headers` |

### 5.6 Reliability and observability

Structured logging with a redaction list; a `requestId` on every response and
error; `/health` (liveness) and `/ready` (readiness, database-dependent) as
separate probes; audit trail as the mutation record of last resort.

---

## 6. Data Requirements

| ID | Requirement | Status |
|---|---|---|
| SRS-DATA-1 | Money is stored as integer halalas; formatting happens only at the boundary. | Met |
| SRS-DATA-2 | Migrations are plain reviewable SQL under `server/drizzle/`, applied by `scripts/migrate.ts`. A passing test run is evidence the migrations work, not merely that the schema file parses. | Met — 13 migrations |
| SRS-DATA-3 | The migration role owns the tables; the application role is a separate non-superuser without `BYPASSRLS`. | Met |
| SRS-DATA-4 | Seed data is the design bundle's own fixtures, so the API returns what the screens were designed against. | Met — `scripts/seed.ts` |
| SRS-DATA-5 | Seeds are idempotent and carry no credentials. | Met |

---

## 7. Verification

A requirement is met when a test proves it, not when a document asserts it.

| Layer | Runner | Scale on 2026-09-03 | On 2026-09-02 |
|---|---|---|---|
| Unit + component | vitest + testing-library | **3,459 passing / 3,459**, 93 files | 3,078 / 3,078 |
| API + integration | vitest + ephemeral PostgreSQL | **2,486 passing / 2,486**, 31 files, 0 failing, 0 skipped | 2,443 / 2,470 |
| E2E — smoke, golden paths, responsive, tablet, security headers | Playwright, desktop and mobile projects | **586 passing / 702**, 116 failing | 260 / 315 |
| Static gates | bespoke | no-fake, tokens, a11y, logical-CSS, a11y-copy, bundle, **headers** passing; **i18n red** | all passing |

The E2E suite roughly doubled because the config gained desktop and mobile
projects, so every spec now runs at both. Failures held at exactly 116 across
that change and across the introduction of the Content-Security-Policy — the
policy added twelve passing specs and broke nothing, which is the only way to
know a directive is safe to tighten.

The i18n gate is red on ten keys added with the current in-flight `DataTable`,
`Pagination`, `PageHeader` and `Breadcrumbs` work — the gate is doing its job.

**Verification environment.** The API suite requires a real PostgreSQL; the
harness drops and rebuilds an ephemeral `salis_auto_test` per file, and
`fileParallelism` is off so that is safe. `tests/global-setup.ts` refuses to
start a run without a database rather than reporting the result as skips,
because a mostly-skipped run reads far too much like a passing one.

**Ratchet.** Coverage thresholds and static-gate baselines may rise and must
never fall to accommodate a regression.

---

## 8. Open Blockers

From `project-control/BLOCKERS.json`, generated:

| ID | Severity | Statement |
|---|---|---|
| BLK-003 | BLOCKER | Three GitHub PATs were exposed and are not confirmed rotated. |
| BLK-004 | CRITICAL | 285 rendered capabilities read fixtures rather than the API. |
| BLK-006 | HIGH | 1 built screen owes its designed mobile layout. |
| BLK-008 | MEDIUM | Tablet verification samples 6 screens, not the full inventory. |

**BLK-002 is cleared.** "No capability is backed by real data" stopped being
true on 2026-09-03; ninety-nine now read the API. BLK-003 is the only blocker
here that no amount of engineering closes — it needs a person to rotate three
credentials.

BLK-004 is the remaining half of a wall whose other half just fell: the server exists,
is migrated, seeded and tested, and the repository seam is built — but
`VITE_API_BASE_URL` is unset, so every screen still reads fixtures. Closing them
is a per-domain migration, not a rewrite.

---

## 9. Conflicts Between Documentation and Code

Recorded because each was found by running something that had never been run.

**9.1 The architecture document describes the server as planned.**
`docs/MASTER_ARCHITECTURE.md` lists Node/Fastify/Drizzle/RLS under "Target" and
"planned". All of it exists: 69 tables, 43 RLS policies, 21 route modules, 2,470
tests. The document predates the server and was never revised.

**9.2 The test strategy reports no test runners.**
`docs/MASTER_TEST_STRATEGY.md` opens "no unit runner, no component tests, no
integration tests, no API tests". There are 3,078 unit and component tests and
2,470 API tests. The file's own note is right: `TEST_STATUS.json` is the live
version and the prose is not.

**9.3 The certification asserted security properties the code contradicted.**
*Resolved 2026-09-03.* `docs/release-blockers.md` §6 marked "No XSS / Injection
Vulnerabilities" **PASS**, citing "CSP headers configured" and a
`security.test.ts` that "validates header presence and content". The CSP was
disabled outright, the frame policy sat on helmet's `SAMEORIGIN` default, and
that suite had never run — it needed a database that was unreachable. The claim
was true only because nothing could check it.

Closed by hardening the code rather than softening the claim: CSP on with
`useDefaults` off, `frameguard: deny`, and the rate-limit headers renamed to the
IETF spelling the API was already documented as returning. The one assertion
that changed direction went *stricter* — `no-referrer` over
`strict-origin-when-cross-origin`. Verified from the response rather than from
the suite:

```
content-security-policy      default-src 'none';frame-ancestors 'none';
                             base-uri 'none';form-action 'none'
x-frame-options              DENY
referrer-policy              no-referrer
strict-transport-security    max-age=15552000; includeSubDomains
x-content-type-options       nosniff
cross-origin-opener-policy   same-origin
cross-origin-resource-policy same-origin
x-powered-by                 (absent)
```

The lesson is kept rather than the finding: a gate that cannot run reports the
absence of evidence as the presence of compliance.

**9.4 Twenty-two API tests asserted a contract the server no longer served.**
*Resolved 2026-09-03.* `collections.test.ts` and `writes.test.ts` expected bare
arrays where the API returns `{rows}`, and sent `customer` where the schema
requires `customerName`. Both files had never executed — they died at login, and
vitest reported their 32 tests as *skipped*, which reads like a deliberate
exclusion rather than two suites falling over.

Settled from the code, not by preference: a body that does not match the schema
is 400, a schema-valid body a rule refuses is 422 — correcting `docs/api.md` and
an `app.ts` fallback that disagreed with every route. The server suite is now
**2,486 passing, 0 failing, 0 skipped** across 31 files.

**9.5 The E2E suite could not run outside CI.**
*Resolved 2026-08-31, consequences outstanding.* `playwright.config.ts` pinned
Chromium to a path that exists only in the CI image, so every spec died at launch
on any other machine — indistinguishable from having no E2E suite. The suite now
runs anywhere, and **116 of 702 specs fail**, mostly on copy assertions that
drifted during the months nobody could run them. Those failures are the debt the
pinned path was hiding, not new breakage.

---

## 10. Traceability

| Requirement area | Primary evidence |
|---|---|
| §3.1–3.2 Identity, authorization | `server/tests/auth.test.ts`, `authz-matrix.test.ts`, `authz-sod.test.ts`, `rbac-parity.test.ts` |
| §3.3 Tenancy | `server/tests/isolation.test.ts`, `server/drizzle/0001_rls.sql` |
| §3.4 Domain rules | `packages/contract/src/rules/`, `finance-integrity`, `inventory-enforcement`, `procurement`, `hr` tests |
| §3.5 Audit | `server/src/audit/audit.ts`, `history-read.test.ts` |
| §4.1 API | `server/tests/api.test.ts`, `errors.test.ts`, `collections.test.ts` |
| §5.1 Performance | `app/scripts/check-bundle.mjs` |
| §5.2 i18n / RTL | `app/scripts/check-i18n.mjs`, `check-logical-css.mjs`, `check-a11y-copy.mjs` |
| §5.3 Accessibility | `app/scripts/check-a11y.mjs`, `docs/A11Y_AUDIT.md` |
| §5.4 Responsive | `app/e2e/tablet.spec.ts`, `responsive.spec.ts` |
| §5.5 Security | `server/tests/security.test.ts` |
| Inventory and status | `project-control/MASTER_REGISTRY.json`, `STATUS.json`, `TEST_STATUS.json` |

---

*Counts taken 2026-09-02 from the generated registry and from test runs on that
date. Regenerate with `npm run registry` in `app/` before relying on them.*
